@if (User.Identity.IsAuthenticated)
{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" href="#">Hello @ViewData["UsersFirstName"]!</a>
        </li>
        <li class="nav-item">
            @Html.ActionLink("Log out", "Logout", "Home", null, new { @class = "nav-link" })
        </li>
    </ul>
}